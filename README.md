# Text-to-Image Lab

A project to build text-to-image generation systems.

Once the virtual environment is set up, register it to the Jupyter interface.
Example:
```
python -m ipykernel install --user --name venv-txt2img --display-name "Python (venv-txt2img)"
```